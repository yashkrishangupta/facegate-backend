{
  "name": "facegate-backend",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix api\" \"npm run dev --prefix web\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}